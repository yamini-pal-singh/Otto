{
  "name": "otto-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "E2E and ASR/STT tests for Otto (gomotto) CSR stage",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:api": "pytest tests/api -v",
    "test:asr": "pytest tests/asr_stt -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.3.0"
  }
}
